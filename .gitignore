node_modules/
dist/
dist-electron/
dist-packages/
release/
*.log
.DS_Store
coverage/
.env
.env.local
*.tsbuildinfo
.vite/
.venv-assets/
__pycache__/
*.py[cod]

# Embedded SQLite databases (including WAL/shm sidecars) and scan lock files
.data/
*.db
*.db-wal
*.db-shm
*.lock

# Generated scan reports (HTML/JSON/NDJSON) — regenerated by every scan, not checked in
packages/vacancy-engine/reports/*
!packages/vacancy-engine/reports/.gitkeep

# Playwright e2e run output — regenerated by every run; baseline screenshots under
# apps/desktop/e2e/**/*-snapshots/ are checked in intentionally and are not excluded here.
apps/desktop/playwright-report/
apps/desktop/test-results/

# Downloaded fresh by scripts/download-vc-redist.mjs before every Windows package build, never
# checked in: it is a ~25 MB third-party binary that changes whenever Microsoft ships an update.
apps/desktop/assets/app-icons/vc_redist.x64.exe
apps/desktop/assets/app-icons/vc_redist.x64.exe.download
